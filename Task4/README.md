# 🗂 File Organizer Tool

Automatically organizes files into folders based on type.

## 🔧 Features

- Organizes files by extension (.jpg, .png, .pdf, .docx)
- Creates folders like `Images/`, `Documents/` inside target directory
- Error handling with try-except
- Works cross-platform using `os` and `shutil`

## 🚀 How to Use

```bash
python organizer.py
```
